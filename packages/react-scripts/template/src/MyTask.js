import React from "react"
import { Button } from '@material-ui/core'

export default () =>  {
  return <div>
      <h1>Hello World</h1>
      <Button onClick={onAdvanceWorkflow}>Next task</Button>
    </div>
}